# Basic
hostname    : ~
config      : ~
arch        : "depth_anything_v2"
model       : "depth_anything_v2_vitl"
data        : ~
data_dir    : ~
root        : ~
project     : ~
variant     : ~
fullname    : ~
save_dir    : ~
weights     : "zoo/vision/depth/depth_anything_v2/depth_anything_v2_vitl/da_2k/depth_anything_v2_vitl_da_2k.pth"
device      : 0
local_rank  : -1
launcher    : ~
epochs      : 200
steps       : ~
imgsz       : 518
resize      : ~
benchmark   : True
save_image  : True
save_debug  : False
use_data_dir: False
exist_ok    : False
verbose     : True

# Model Specific
encoder     : "vitl"
features    : 256
out_channels: [256, 512, 1024, 1024]
pred_only   : True
format      : 2  # 0: grayscale, 1: color, 2: both
